import React from 'react'
import MainLayout from '../Components/MainLayout';

const Rooms = () => {
  return (
    <MainLayout style={{display: "flex", alignItems: "center", justifyContent: "center"}}>Rooms</MainLayout>
  )
}

export default Rooms;